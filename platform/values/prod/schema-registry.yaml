# Schema Registry prod overrides
image:
  registry: public.ecr.aws
  repository: bitnami/schema-registry
  tag: 8.0.0-debian-12-r4
kafka:
  enabled: true
  replicaCount: 3
configurationOverrides:
  kafkastore.bootstrap.servers: PLAINTEXT://redpanda.messaging.svc.cluster.local:9092
  kafkastore.topic: _schemas
