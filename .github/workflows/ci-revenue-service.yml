name: CI · Revenue Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/revenue-service/**'
      - 'schema/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/revenue-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/revenue-service'
