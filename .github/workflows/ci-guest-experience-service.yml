name: CI · Guest Experience Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/guest-experience-service/**'
      - 'schema/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/guest-experience-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/guest-experience-service'
