name: CI · Reservations Command Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/reservations-command-service/**'
      - 'schema/**'
      - 'package.json'
      - 'lerna.json'
  pull_request:
    paths:
      - 'Apps/reservations-command-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/reservations-command-service'
