name: CI · Accounts Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/accounts-service/**'
      - 'schema/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/accounts-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/accounts-service'
      has-tests: false
