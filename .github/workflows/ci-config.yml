name: CI · Config

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/config/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/config/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/config'
      has-tests: false
