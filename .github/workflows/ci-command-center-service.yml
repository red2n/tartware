name: CI · Command Center Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/command-center-service/**'
      - 'Apps/command-center-shared/**'
      - 'schema/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/command-center-service/**'
      - 'Apps/command-center-shared/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/command-center-service'
