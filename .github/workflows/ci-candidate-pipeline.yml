name: CI · Candidate Pipeline

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/candidate-pipeline/**'
      - 'schema/**'
      - 'package.json'
      - 'lerna.json'
  pull_request:
    paths:
      - 'Apps/candidate-pipeline/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/candidate-pipeline'
