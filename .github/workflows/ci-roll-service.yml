name: CI · Roll Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/roll-service/**'
      - 'schema/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/roll-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/roll-service'
