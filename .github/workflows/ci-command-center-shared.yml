name: CI · Command Center Shared

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/command-center-shared/**'
      - 'schema/**'
      - 'package.json'
      - 'lerna.json'
  pull_request:
    paths:
      - 'Apps/command-center-shared/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/command-center-shared'
