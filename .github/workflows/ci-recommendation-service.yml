name: CI · Recommendation Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/recommendation-service/**'
      - 'schema/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/recommendation-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/recommendation-service'
