name: CI · Rooms Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/rooms-service/**'
      - 'schema/**'
      - 'package.json'
      - 'lerna.json'
  pull_request:
    paths:
      - 'Apps/rooms-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/rooms-service'
