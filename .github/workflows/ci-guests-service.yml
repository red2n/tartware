name: CI · Guests Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/guests-service/**'
      - 'schema/**'
      - 'package.json'
      - 'lerna.json'
  pull_request:
    paths:
      - 'Apps/guests-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/guests-service'
