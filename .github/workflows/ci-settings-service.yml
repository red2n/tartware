name: CI · Settings Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/settings-service/**'
      - 'schema/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/settings-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/settings-service'
