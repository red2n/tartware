name: CI · Housekeeping Service

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/housekeeping-service/**'
      - 'schema/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/housekeeping-service/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/housekeeping-service'
