name: CI · Telemetry

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/telemetry/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/telemetry/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/telemetry'
