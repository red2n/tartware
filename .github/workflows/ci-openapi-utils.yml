name: CI · OpenAPI Utils

on:
  push:
    branches: [main, develop, deployment]
    paths:
      - 'Apps/openapi-utils/**'
      - 'schema/**'
      - 'package.json'
      - 'nx.json'
  pull_request:
    paths:
      - 'Apps/openapi-utils/**'
      - 'schema/**'
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/package-ci.yml
    with:
      package-scope: '@tartware/openapi'
