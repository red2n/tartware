// Shared test setup. Kept minimal — the shared vitest config already forces
// LOG_LEVEL=silent, and these tests exercise pure functions and Fastify
// instances built in-process, so no external services are needed.
export {};
